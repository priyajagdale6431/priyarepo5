name: Run streamlit job
on:
    push:
        branches:
        - main

jobs:
   ci-pipeline:
       runs_on: ubuntu-latest

       steps:
        - name: checkout code
          uses: actions/checkout@v2

        - name: setup python
          run: echo "welcome to avd"

