name: Run Python Program
on:
    push:
        branches:
              - main
jobs:
   ci-pipeline:
       runs_on: ubuntu-latest

       steps:
        - name: checkout code
          uses: actions/checkout@v2

        - name: print massege
          run: echo 'welcome to avd'

